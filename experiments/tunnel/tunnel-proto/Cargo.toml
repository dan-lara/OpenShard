[package]
name = "tunnel-proto"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = { workspace = true }
tonic = { workspace = true }

[build-dependencies]
tonic-build = "0.11"
protoc-bin-vendored = "3"
